<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Workout Feed</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <header>
        <h1>Workout Feed</h1>
        <h3>See what others are doing today!</h3>
    </header>
    <br><br>

    <div class="container">
        {% for post in posts %}
        <div class="post">
            <h2>{{ post['title'] }}</h2>
            <p><strong>Posted by:</strong> {{ post['display_name'] }}</p>
            <p><strong>Favourite Workout:</strong> {{ post['favourite'] }}</p>
            <p><strong>Routine:</strong> {{ post['routine'] }}</p>
        </div>
        <br>
        {% endfor %}
    </div>

    <footer>
        <a href="{{ url_for('home') }}">Back to Home</a>
    </footer>
</body>
</html>
